CREATE SCHEMA painel_siads;

-- Create the table
CREATE TABLE painel_siads.[table] (
    [column] INTEGER
);

-- Create the table
--DROP TABLE painel_siads.table;

-- Insert a value into the table
INSERT INTO painel_siads.[table] ([column]) VALUES (0);